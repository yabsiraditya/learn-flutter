import 'package:flutter/material.dart';

class FontFeature extends StatelessWidget {
  const FontFeature({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
        title: Text("Flutter Typography"),
        backgroundColor: Colors.lime,
      ),
    );
  }
}